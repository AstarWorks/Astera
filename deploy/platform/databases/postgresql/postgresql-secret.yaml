---
# Placeholder. Real values are provided via overlay in `private/infra/overlays/<env>/`.
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: databases
type: Opaque
stringData:
  postgres-user: "postgres"
  postgres-password: "REPLACE_ME"
  postgres-db: "astera_db"
  app-user: "astera_app"
  app-password: "REPLACE_ME"
