---
# Placeholder. Real URL is overlayed from private/infra/.
apiVersion: v1
kind: Secret
metadata:
  name: discord-webhook-secret
  namespace: databases
type: Opaque
stringData:
  webhook-url: "REPLACE_ME"
