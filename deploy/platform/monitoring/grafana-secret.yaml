---
# Placeholder. Real credentials live in overlay (sealed-secrets / SOPS).
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: monitoring
type: Opaque
stringData:
  admin-user: "admin"
  admin-password: "REPLACE_ME"
  datasource-user: "astera_app"
  datasource-password: "REPLACE_ME"
