---
# Placeholder secret. Real values live in `private/infra/overlays/<env>/`
# as sealed-secrets or SOPS-encrypted blobs. Do NOT commit real passwords here.
apiVersion: v1
kind: Secret
metadata:
  name: minecraft-rcon
  namespace: astera
type: Opaque
stringData:
  lobby-password: "REPLACE_ME"
  game-password: "REPLACE_ME"
